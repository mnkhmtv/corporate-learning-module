env: development

server:
  port: "8080"
  read_timeout: 10s
  write_timeout: 10s

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: training_db
  sslmode: disable
  auto_migrate: true

auth:
  jwt_secret: your-super-secret-key-change-in-production
  token_ttl: 24h
